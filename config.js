// Configuration constants and defaults for the extension

const config = {
    apiUrl: 'https://api.example.com',
    timeout: 5000, // Request timeout in milliseconds
    enableFeatureX: true,
    defaultLanguage: 'en',
    supportEmail: 'support@example.com'
};

export default config;